"use strict";

(function () {
    angular
        .module("BookExchangeApp")
        .controller("HomeauthController", homeauthController);

    function homeauthController() {
        // No event handlers currently required.
    }
})();